import React from 'react';

const description = "HAloo"

const RenderingElement = () => {
    return (
        <div>
            {description}
        </div>
    );
}

export default RenderingElement;
